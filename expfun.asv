function Y = expfun(param, t)

    y0 = param(1);
    A0 = param(2);
    k1 = param(3);
    c  = param(4);

    [~, yout] = ode45(@difeq, t, y0);
    
    Y = A0 .* yout + c;
    
    function dy = difeq(~, y)
        dy = -k1*y(1);
    end
end